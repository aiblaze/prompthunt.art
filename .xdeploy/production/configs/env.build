# 构建时环境变量
# 这些变量只在构建时使用，不会被部署到服务器

# 基本环境配置
NODE_ENV=production

# Node.js 配置
NODE_OPTIONS=--max-old-space-size=4096

# 构建配置
BUILD_TARGET=server
BUILD_MODE=production

# 请在下方添加应用特定的构建环境变量
# APP_BUILD_VAR=value 